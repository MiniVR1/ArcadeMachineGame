using System.Linq;
using UnityEngine;

public class NewGlitchesManager2 : MonoBehaviour
{
    // THIS CODE HAS BEEN MADE OBSOLETE BY NEWGLITCHES.CS
    /*
    public LevelManager levelManager;
    [Header("Glitch Refs")]
    public NewGlitches[] level1Glitches;
    public NewGlitches[] level2Glitches;
    public NewGlitches[] level3Glitches;
    [HideInInspector]public int glitchEnabled = 0; //Reset this to 0 when level up!

  

    void OnEnable()
    {
        BaseballHit.Instance.hitTop += ToggleGlitch;
    }

    void ToggleGlitch()
    {
        switch (levelManager.level)
        {
            case CurrentLevel.Level1:
                CycleGlitch(level1Glitches);
                break;
            case CurrentLevel.Level2:
                CycleGlitch(level2Glitches);
                break;
            case CurrentLevel.Level3:
                CycleGlitch(level3Glitches);
                break;
        }
    }
    
    void CycleGlitch(NewGlitches[] GlitchArray)
    {
        GlitchArray[glitchEnabled].ToggleGlitch();
        glitchEnabled++;
        if (glitchEnabled >= GlitchArray.Length)
        {
            glitchEnabled = 0;

        }
    }
    */

}
